<Window x:Class="WPF_BanHang.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_BanHang"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d"
        x:Name="window"
        Title="MainWindow" Height="720" Width="1280"
        
        xmlns:uc="clr-namespace:WPF_BanHang.UserControl_BanHang"
        
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        
        WindowState="Maximized"
        DataContext="{StaticResource mainVM}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction 
                Command="{Binding loadedwindowcommand}" 
                CommandParameter="{Binding ElementName=window}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#233C4F">
            <StackPanel VerticalAlignment="Center">
                <Image Source="Hinh/Logo.png" Width="80"/>
                <TextBlock Foreground="White" HorizontalAlignment="Center" FontSize="20">CỬA HÀNG TIỆN LỢI</TextBlock>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Background="#14212B">

                <Grid Grid.Row="0" VerticalAlignment="Top" Background="BlanchedAlmond">
                </Grid>
                <Grid Grid.Row="1">
                    <StackPanel>
                        <Button Height="50" Content="ORDER" Margin="0 20 0 0" BorderBrush="Transparent" Background="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Setter Property="FontWeight" Value="Normal"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="0">
                                            <Setter Property="FontWeight" Value="Bold"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnOrderCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <Button Height="50" Content="THỐNG KÊ" Margin="0 15 0 0" BorderBrush="Transparent" Background="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Setter Property="FontWeight" Value="Normal"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="1">
                                            <Setter Property="FontWeight" Value="Bold"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnThongkeCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <Button Height="50" Content="NHẬP KHO" Margin="0 15 0 0" BorderBrush="Transparent" Background="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="2">
                                            <Setter Property="FontWeight" Value="Bold"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnNhapKhoCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <Button  Height="50" Content="NHÂN VIÊN" Margin="0 15 0 0" BorderBrush="Transparent" Background="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="3">
                                            <Setter Property="FontWeight" Value="Bold"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnNhanVienCommand}">
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                        </Button>
                    </StackPanel>
                    <Button Height="50" Content="ĐỔI MẬT KHẨU" Margin="0 0 0 65" VerticalAlignment="Bottom" BorderBrush="Transparent" Background="Transparent">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="1">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Height="50" Content="ĐĂNG XUẤT" VerticalAlignment="Bottom" BorderBrush="Transparent" Background="Transparent">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNangNS}" Value="1">
                                        <Setter Property="FontWeight" Value="Bold"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
            <Grid Grid.Column="1">
                <!--CONTENT-->
                <Grid>
                    <Grid x:Name="order">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNang}" Value="1">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
<<<<<<< HEAD
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="4*"></RowDefinition>
                                <RowDefinition Height="2*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <Rectangle Margin="3" RadiusY="10" RadiusX="10" Fill="White">
                                    <Rectangle.Effect>
                                        <DropShadowEffect BlurRadius="15" Direction="0" RenderingBias="Quality" ShadowDepth="1" Color="#FFBBBBBB"/>
                                    </Rectangle.Effect>
                                </Rectangle>
                                <ListView ItemsSource="{Binding tonkhoxlist}">
                                    <!--ĐỊNH DẠNG NỘI DUNG-->
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                            <Setter Property="FontSize" Value="15"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.View>
                                        <GridView>
                                            <!--ĐỊNH DẠNG HEADER-->
                                            <GridView.ColumnHeaderContainerStyle>
                                                <Style TargetType="GridViewColumnHeader">
                                                    <Setter Property="FontSize" Value="20"/>
                                                    <Setter Property="Margin" Value="10"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </Style>
                                            </GridView.ColumnHeaderContainerStyle>
                                            <GridViewColumn Width="70" Header="STT" DisplayMemberBinding="{Binding STT}"/>

                                            <GridViewColumn Width="200" Header="Barcode" DisplayMemberBinding="{Binding barcode }"/>

                                            <GridViewColumn Width="430" Header="Tên sản phẩm" DisplayMemberBinding="{Binding ten }"/>

                                            <GridViewColumn Width="120" Header="Số lượng" DisplayMemberBinding="{Binding soluong}"/>

                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </Grid>
                            <!--Listview NHẬP KHO-->
                            <Grid Grid.Row="1">
                                <materialDesign:Card Margin="10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0">
                                            <TextBox HorizontalAlignment="Center" Margin="0 0 150 0" materialDesign:HintAssist.Hint="Mã sản phẩm" TextWrapping="Wrap" Width="120" Height="25" VerticalAlignment="Center"/>
                                            <TextBox HorizontalAlignment="Center" Margin="0 0 -150 0" materialDesign:HintAssist.Hint="Số lượng" TextWrapping="Wrap" Width="120" Height="25" VerticalAlignment="Center"/>
                                        </Grid>
                                        <Grid Grid.Row="1">
                                            <Button Command="{Binding themsanphamcommand}" Height="60" Width="120" Content="Thêm" HorizontalAlignment="Center" Margin="0,0,300,0" VerticalAlignment="Center"/>
                                            <Button Command="{Binding suasanphamcommand}" Height="60" Width="120" Content="Sửa" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            <Button Height="60" Width="120" Content="Xóa" HorizontalAlignment="Center" Margin="0,0,-300,0" VerticalAlignment="Center"/>

                                        </Grid>
                                    </Grid>
                                </materialDesign:Card>
                            </Grid>
                        </Grid>
                    </Grid>
                    <Grid x:Name="thongke">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNang}" Value="1">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid>
                            <TabControl Background="{Binding}" TabStripPlacement="Top" BorderThickness="1">
                                <TabControl.Resources>
                                    <Style TargetType="TabItem">
                                        <Setter Property="Background" Value="AntiqueWhite"/>
                                        <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        <Setter Property="FontSize" Value="20"/>
=======
                        <Grid DataContext="{StaticResource nkVM}">
                            <ListView ItemsSource="{Binding tonkhoxlist}" >
                                <!--ĐỊNH DẠNG NỘI DUNG-->
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        <Setter Property="FontSize" Value="15"/>
>>>>>>> 73c4c4e68df92d76d5a29dc95814816f61d8d21c
                                    </Style>
                                </TabControl.Resources>
                                <!--HÓA ĐƠN-->
                                <TabItem Header="HÓA ĐƠN" Height="50" Width="250">
                                    <Grid>

                                        <TabControl Width="auto" BorderThickness="5">
                                            <TabItem Header="Khách hàng" Height="50">
                                                <ListView ItemsSource="{Binding nhanvienlist}">
                                                    <!--ĐỊNH DẠNG NỘI DUNG-->
                                                    <ListView.ItemContainerStyle>
                                                        <Style TargetType="ListViewItem">
                                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                                            <Setter Property="FontSize" Value="15"/>
                                                        </Style>
                                                    </ListView.ItemContainerStyle>
                                                    <ListView.View>
                                                        <GridView>
                                                            <!--ĐỊNH DẠNG HEADER-->
                                                            <GridView.ColumnHeaderContainerStyle>
                                                                <Style TargetType="GridViewColumnHeader">
                                                                    <Setter Property="FontSize" Value="20"/>
                                                                    <Setter Property="Margin" Value="10"/>
                                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </Style>
                                                            </GridView.ColumnHeaderContainerStyle>
                                                            <GridViewColumn Width="200" Header="Mã hóa đơn" DisplayMemberBinding="{Binding Manv }"/>
                                                            <GridViewColumn Width="430" Header="Tên khách hàng" DisplayMemberBinding="{Binding ten }"/>
                                                            <GridViewColumn Width="400" Header="Tổng tiền" DisplayMemberBinding="{Binding diachi}"/>
                                                            <GridViewColumn Width="195" Header="Nhân viên" DisplayMemberBinding="{Binding chucvu}"/>
                                                        </GridView>
                                                    </ListView.View>
                                                </ListView>
                                            </TabItem>
                                            <TabItem Header="Nhà phân phối">
                                                <ListView ItemsSource="{Binding nhanvienlist}">
                                                    <!--ĐỊNH DẠNG NỘI DUNG-->
                                                    <ListView.ItemContainerStyle>
                                                        <Style TargetType="ListViewItem">
                                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                                            <Setter Property="FontSize" Value="15"/>
                                                        </Style>
                                                    </ListView.ItemContainerStyle>
                                                    <ListView.View>
                                                        <GridView>
                                                            <!--ĐỊNH DẠNG HEADER-->
                                                            <GridView.ColumnHeaderContainerStyle>
                                                                <Style TargetType="GridViewColumnHeader">
                                                                    <Setter Property="FontSize" Value="20"/>
                                                                    <Setter Property="Margin" Value="10"/>
                                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                                    <Setter Property="IsEnabled" Value="False"/>
                                                                </Style>
                                                            </GridView.ColumnHeaderContainerStyle>
                                                            <GridViewColumn Width="200" Header="Mã nhà phân phối" DisplayMemberBinding="{Binding Manv }"/>
                                                            <GridViewColumn Width="430" Header="Tên sản phẩm" DisplayMemberBinding="{Binding ten }"/>
                                                            <GridViewColumn Header="Đơn giá" DisplayMemberBinding="{Binding Pass}"/>
                                                            <GridViewColumn Header="Số lượng" DisplayMemberBinding="{Binding ngaysinh}"/>
                                                            <GridViewColumn Width="400" Header="Tổng tiền" DisplayMemberBinding="{Binding diachi}"/>
                                                            <GridViewColumn Width="195" Header="Ngày giao" DisplayMemberBinding="{Binding chucvu}"/>
                                                        </GridView>
                                                    </ListView.View>
                                                </ListView>
                                            </TabItem>
                                        </TabControl>

                                    </Grid>
                                </TabItem>
                                <!--SẢN PHẨM-->
                                <TabItem Header="SẢN PHẨM" Height="50" Width="250">

                                    <Grid Background="White">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="auto"></RowDefinition>
                                            <RowDefinition></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0">
                                            <ComboBox
  materialDesign:HintAssist.Hint="Theo số lượng" Width="200" HorizontalAlignment="Left" Margin="10">
                                                <ComboBoxItem>
                                                    Mặc định
                                                </ComboBoxItem>
                                                <ComboBoxItem>
                                                    Ít đến nhiều
                                                </ComboBoxItem>
                                                <ComboBoxItem>
                                                    Nhiều đến ít
                                                </ComboBoxItem>


                                            </ComboBox>
                                            <DatePicker
                                            Margin="0 0 300 0"
  Width="150"
  materialDesign:HintAssist.Hint="Từ ngày"
  Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                                            <DatePicker
                                            Margin="100 0 0 0"
  Width="150"
  materialDesign:HintAssist.Hint="Đến ngày"
  Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                                            <Button
                                             Margin="500 10 0 0"
    Style="{StaticResource MaterialDesignRaisedButton}"
    Width="100"
                                            Content="Tìm"
    ToolTip="Resource name: MaterialDesignRaisedButton">

                                            </Button>
                                        </Grid>
                                        <Grid Grid.Row="1">
                                            <ListView ItemsSource="{Binding nhanvienlist}">
                                                <!--ĐỊNH DẠNG NỘI DUNG-->
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">
                                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                                        <Setter Property="FontSize" Value="15"/>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                                <ListView.View>
                                                    <GridView>
                                                        <!--ĐỊNH DẠNG HEADER-->
                                                        <GridView.ColumnHeaderContainerStyle>
                                                            <Style TargetType="GridViewColumnHeader">
                                                                <Setter Property="FontSize" Value="20"/>
                                                                <Setter Property="Margin" Value="10"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="IsEnabled" Value="False"/>
                                                            </Style>
                                                        </GridView.ColumnHeaderContainerStyle>
                                                        <GridViewColumn Width="200" Header="Barcode" DisplayMemberBinding="{Binding Manv }"/>
                                                        <GridViewColumn Width="430" Header="Tên sản phẩm" DisplayMemberBinding="{Binding ten }"/>
                                                        <GridViewColumn Header="Đơn giá" DisplayMemberBinding="{Binding Pass}"/>
                                                        <GridViewColumn Header="Số lượng" DisplayMemberBinding="{Binding ngaysinh}"/>
                                                        <GridViewColumn Width="400" Header="Tổng tiền" DisplayMemberBinding="{Binding diachi}"/>
                                                        <GridViewColumn Width="195" Header="Nhân viên" DisplayMemberBinding="{Binding chucvu}"/>
                                                    </GridView>
                                                </ListView.View>
                                            </ListView>
                                        </Grid>
                                    </Grid>

                                </TabItem>
                                <!--LỊCH SỬ-->
                                <TabItem Header="LỊCH SỬ" Height="50" Width="250">
                                    <Grid Background="White"/>
                                </TabItem>
                                <!--DOANH THU-->
                                <TabItem Header="DOANH THU" Height="50" Width="250">
                                    <Grid Background="White"/>

                                </TabItem>
                            </TabControl>
                        </Grid>
                    </Grid>
                    <Grid x:Name="nhapkho">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNang}" Value="2">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition ></RowDefinition>

                            </Grid.RowDefinitions>

                            <Grid>

                                <Rectangle Margin="3" RadiusY="10" RadiusX="10" Fill="White">
                                    <Rectangle.Effect>
                                        <DropShadowEffect BlurRadius="15" Direction="0" RenderingBias="Quality" ShadowDepth="1" Color="#FFBBBBBB"/>
                                    </Rectangle.Effect>
                                </Rectangle>
                                <Grid Height="110" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0">
                                        <TextBox HorizontalAlignment="Center" Margin="10 0 50 0"  materialDesign:HintAssist.Hint="Mã sản phẩm" TextWrapping="Wrap" Width="120" Height="25" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Grid Grid.Column="1">
                                        <TextBox HorizontalAlignment="Center" Margin="10 0 50 0"  materialDesign:HintAssist.Hint="Số lượng" TextWrapping="Wrap" Width="120" Height="25" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Grid Grid.Column="2">
                                        <Button Command="{Binding themsanphamcommand}" Margin="10 0 50 0" Height="60" Width="120" Content="Thêm" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                    </Grid>
                                    <Grid Grid.Column="3">
                                        <Button Command="{Binding suasanphamcommand}" Margin="10 0 50 0" Height="60" Width="120" Content="Sửa" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                    </Grid>
                                    <Grid Grid.Column="4">
                                        <Button Height="60" Width="120" Content="Xóa" Margin="10 0 50 0" HorizontalAlignment="Center"  VerticalAlignment="Center"/>

                                    </Grid>
                                </Grid>
                                <ListView Margin="0 100 0 0" ItemsSource="{Binding tonkhoxlist}">
                                    <!--ĐỊNH DẠNG NỘI DUNG-->
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                            <Setter Property="FontSize" Value="15"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.View>
                                        <GridView>
                                            <!--ĐỊNH DẠNG HEADER-->
                                            <GridView.ColumnHeaderContainerStyle>
                                                <Style TargetType="GridViewColumnHeader">
                                                    <Setter Property="FontSize" Value="20"/>
                                                    <Setter Property="Margin" Value="10"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </Style>
                                            </GridView.ColumnHeaderContainerStyle>
                                            <GridViewColumn Width="70" Header="STT" DisplayMemberBinding="{Binding STT}"/>

                                            <GridViewColumn Width="200" Header="Barcode" DisplayMemberBinding="{Binding barcode }"/>

                                            <GridViewColumn Width="430" Header="Tên sản phẩm" DisplayMemberBinding="{Binding ten }"/>

                                            <GridViewColumn Width="120" Header="Số lượng" DisplayMemberBinding="{Binding soluong}"/>

                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </Grid>
                            <!--Listview NHẬP KHO-->

                        </Grid>
                    </Grid>
                    <Grid x:Name="nhanvien" Background="Transparent">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChucNang}" Value="3">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition></RowDefinition>
                                <RowDefinition Height="auto"></RowDefinition>
                            </Grid.RowDefinitions>
                            <!--HIỂN THỊ-->
                            <Grid Grid.Row="0">
                                <Rectangle Margin="3" RadiusY="10" RadiusX="10" Fill="White">
                                    <Rectangle.Effect>
                                        <DropShadowEffect BlurRadius="15" Direction="0" RenderingBias="Quality" ShadowDepth="1" Color="#FFBBBBBB"/>
                                    </Rectangle.Effect>
                                </Rectangle>
                                <ListView Margin="0 100 0 0" ItemsSource="{Binding nhanvienlist}" SelectedItem="{Binding SelectedItem, Mode=OneWayToSource}">
                                    <!--ĐỊNH DẠNG NỘI DUNG-->
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                            <Setter Property="FontSize" Value="15"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.View>
                                        <GridView>
                                            <!--ĐỊNH DẠNG HEADER-->
                                            <GridView.ColumnHeaderContainerStyle>
                                                <Style TargetType="GridViewColumnHeader">
                                                    <Setter Property="FontSize" Value="20"/>
                                                    <Setter Property="Margin" Value="10"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </Style>
                                            </GridView.ColumnHeaderContainerStyle>
                                            <GridViewColumn Width="70" Header="ID" DisplayMemberBinding="{Binding Manv }"/>
                                            <GridViewColumn Width="140" Header="Tên nhân viên" DisplayMemberBinding="{Binding ten }"/>
                                            <GridViewColumn Header="Ngày bắt đầu làm" DisplayMemberBinding="{Binding ngaysinh}"/>
                                            <GridViewColumn Width="400" Header="Địa chỉ" DisplayMemberBinding="{Binding diachi}"/>
                                            <GridViewColumn Width="170" Header="Chức vụ" DisplayMemberBinding="{Binding chucvu}"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </Grid>
                            <!--CHỈNH SỬA-->
                            <Grid Grid.Row="0">

                                <Grid VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition></RowDefinition>

                                    </Grid.RowDefinitions>
                                    <Grid >
                                        <Grid >
                                            <Button Command="{Binding themnhanviencommand}" Height="60" Width="120" Content="Thêm" HorizontalAlignment="Center" Margin="0,0,300,0" VerticalAlignment="Center"/>
                                            <Button Command="{Binding suanhanviencommand}" Height="60" Width="120" Content="Sửa" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            <Button Command="{Binding enablecommand}" Height="60" Width="120" Content="Enable" HorizontalAlignment="Center" Margin="0,0,-300,0" VerticalAlignment="Center"/>
                                            <Button Command="{Binding disablecommand}" Height="60" Width="120" Content="Disable" HorizontalAlignment="Center" Margin="0,0,-600,0" VerticalAlignment="Center"/>

                                        </Grid>
                                    </Grid>

                                </Grid>

                            </Grid>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="5.5*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1">
                
            </Grid>
        </Grid>
    </Grid>
</Window>
