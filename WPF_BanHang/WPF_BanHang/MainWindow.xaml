<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_BanHang"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:uc="clr-namespace:WPF_BanHang.UserControl_BanHang"               
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
     xmlns:view="clr-namespace:WPF_BanHang.view" x:Class="WPF_BanHang.MainWindow"
        mc:Ignorable="d"
        x:Name="window"
        Icon="Hinh\Logo.png"
        Title="Cửa hàng tiện lợi" Height="720" Width="1280"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStyle="None"
        WindowState="Normal"
        WindowStartupLocation="CenterScreen"
        
        ResizeMode="CanResize"
        DataContext="{StaticResource mainVM}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding SetupQuyenCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="15*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Black">
            <!--TIÊU ĐỀ-->
            <StackPanel VerticalAlignment="Center">
                <TextBlock Foreground="White" HorizontalAlignment="Center" FontSize="30"><Run Text="CỬA HÀNG TIỆN LỢI"/></TextBlock>
            </StackPanel>
            <!--ĐỒNG HỒ-->
            <Grid  VerticalAlignment="Center" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="dongho1" Foreground="White" FontSize="35"  />
                    <Image Source="Hinh/Logo.png" Width="40" Margin="20 0 0 0"/>
                </StackPanel>
            </Grid>


            <!--NỘI DUNG-->
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="Goldenrod">


                <Grid Grid.Row="0" VerticalAlignment="Top" Background="BlanchedAlmond"/>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>


                        <!--NÚT ORDER-->
                        <Button Height="50"  Margin="0 15 0 0" BorderBrush="Transparent" Background="Transparent">

                            <Button.Style>
                                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Setter Property="FontWeight" Value="Normal"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="0">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnOrderCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                            <DockPanel>
                                <Image  Width="40" Height="40" Source="UserControl_BanHang/Icons/icon-order.png" RenderTransformOrigin="-0.19,0.542"/>
                                <TextBlock VerticalAlignment="Center" FontSize="20" Margin="0 7 0 0" Text="ORDER"/>
                            </DockPanel>

                        </Button>
                        <!--NÚT THỐNG KÊ-->
                        <Button Height="50"  Margin="0 15 0 0" BorderBrush="Transparent" Background="Transparent">
                            <Button.Style>
                                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="1">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnThongKeCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <DockPanel>
                                <Image Width="40" Height="40" Source="UserControl_BanHang/Icons/Thongke.png"/>
                                <TextBlock VerticalAlignment="Center" FontSize="20" Margin="0 7 0 0"><Run Text="THỐNG KÊ"/></TextBlock>
                            </DockPanel>
                        </Button>
                        <!--NÚT SẢN PHẨM-->
                        <Button Height="50"  Margin="0 15 0 0" BorderBrush="Transparent" Background="Transparent">
                            <Button.Style>
                                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="2">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnNhapKhoCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <DockPanel>
                                <Image Width="40" Height="40" Source="UserControl_BanHang/Icons/Kho.png"/>
                                <TextBlock VerticalAlignment="Center" FontSize="20" Margin="0 7 0 0" Text="SẢN PHẨM"/>
                            </DockPanel>
                        </Button>
                        <!--NÚT NHẬP HÀNG-->
                        <Button Height="50"  Margin="0 15 0 0" BorderBrush="Transparent" Background="Transparent">
                            <Button.Style>
                                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="4">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnNhapHangCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <DockPanel>
                                <Image Width="40" Height="40" Source="UserControl_BanHang/Icons/ThemSP.png"/>
                                <TextBlock VerticalAlignment="Center" FontSize="20" Margin="0 7 0 0" Text="NHẬP HÀNG"/>
                            </DockPanel>
                        </Button>
                        <!--NÚT NHÂN VIÊN-->
                        <Button  Height="50"  Margin="0 15 0 0" BorderBrush="Transparent" Background="Transparent">
                            <Button.Style>
                                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChucNang}" Value="3">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding BtnNhanVienCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>

                            <DockPanel>
                                <Image Width="40" Height="40" Source="Hinh/Nhanvien.png"/>
                                <TextBlock VerticalAlignment="Center" FontSize="20" Margin="0 7 0 0"><Run Text="NHÂN VIÊN"/></TextBlock>
                            </DockPanel>

                        </Button>

                    </StackPanel>
                    <!--NÚT ĐỔI MẬT KHẨU-->
                    <Button Command="{Binding dmkcommand}" Height="50" FontSize="20"  Margin="0 0 0 100" VerticalAlignment="Bottom" BorderBrush="Transparent" Background="Transparent">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"/>
                            </Style>
                        </Button.Style>
                        <DockPanel>
                            <Image Width="40" Height="40" Source="UserControl_BanHang/Icons/icon-password.png"/>
                            <TextBlock Text="ĐỔI MẬT KHẨU"  VerticalAlignment="Center" FontSize="20" Margin="0 7 0 0"/>
                        </DockPanel>
                    </Button>
                    <!--NÚT ĐĂNG XUẤT-->
                    <Button Height="50"  VerticalAlignment="Bottom" BorderBrush="Transparent" Background="Transparent"
                            Margin="0 0 0 30"
                            Command="{Binding closecommand}"
                            CommandParameter="{Binding ElementName=window}">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="FontWeight" Value="Normal"/>
                            </Style>
                        </Button.Style>

                        <DockPanel>
                            <Image Width="40" Height="40" Source="UserControl_BanHang/Icons/logout.png"/>
                            <TextBlock VerticalAlignment="Center" FontSize="20" Margin="0 7 0 0"><Run Text="ĐĂNG XUẤT"/></TextBlock>
                        </DockPanel>

                    </Button>
                </Grid>
            </Grid>
            <Grid Grid.Column="1">
                <!--CONTENT-->
                <Grid x:Name="order">
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChucNang}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid DataContext="{StaticResource orderVM}">
                  
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition />
                            <RowDefinition Height="80"/>
                        </Grid.RowDefinitions>

                        <!--BARCODE-->
                        <Grid Grid.Row="0" Background="DarkGoldenrod">
                            <StackPanel Orientation="Horizontal">
                                <TextBox
                                    x:Name="BARCODE" Margin="10"
                                    FontSize="20" Width="200" Height="30" 
                                    Background="White"
                                    materialDesign:HintAssist.Hint="Barcode"
                                    >
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="TextChanged">
                                            <i:InvokeCommandAction 
                                                    Command="{Binding BarcodeChangedcommand}" 
                                                    CommandParameter="{Binding ElementName=BARCODE}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>

                                <TextBlock Margin="30 0 0 0" Text="Số lượng: " FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" Foreground="White" />
                                <TextBox x:Name="SOLUONG" FontSize="20" Width="200" Height="30" Text="{Binding soluongsp}" Background="White"/>
                                <!--NÚT NHẬP SỐ LƯỢNG-->
                                <Button Content="Nhập" Background="#ff6f00" BorderBrush="Transparent" Margin="10 0 0 0" Width="120" Height="30" Command="{Binding editsoluong}" CommandParameter="{Binding ElementName=SOLUONG}"/>
                            </StackPanel>
                        </Grid>
                        <!--THÔNG TIN SẢN PHẨM KHÁCH MUA-->
                        <Grid Grid.Row="1"  Background="PaleGoldenrod">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="31*"/>
                                <ColumnDefinition Width="5*"/>
                            </Grid.ColumnDefinitions>
                            <ListView ItemsSource="{Binding orderlist, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" Grid.ColumnSpan="2">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:InvokeCommandAction 
                                        Command="{Binding unloadcommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                                <!--ĐỊNH DẠNG NỘI DUNG-->
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="{x:Type ListViewItem}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        <Setter Property="FontSize" Value="15"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.View>
                                    <GridView>
                                        <!--ĐỊNH DẠNG HEADER-->
                                        <GridView.ColumnHeaderContainerStyle>
                                            <Style TargetType="{x:Type GridViewColumnHeader}">
                                                <Setter Property="FontSize" Value="20"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="Margin" Value="10"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="IsEnabled" Value="True"/>
                                                <Setter Property="Width" Value="auto"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderThickness" Value="5"/>
                                            </Style>
                                        </GridView.ColumnHeaderContainerStyle>
                                        <!--<GridViewColumn Header="Hình ảnh">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Image Source="{Binding hinhsp}" Width="100" Height="100"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>-->
                                        <GridViewColumn Header="Tên sản phẩm" DisplayMemberBinding="{Binding tensp}"/>
                                        <GridViewColumn Header="Đơn giá" DisplayMemberBinding="{Binding dongia, StringFormat=C, ConverterCulture=vi }"/>
                                        <GridViewColumn Header="Số lượng" DisplayMemberBinding="{Binding soluong}"/>
                                        <GridViewColumn Header="Tổng tiền" DisplayMemberBinding="{Binding tongtien, StringFormat=C, ConverterCulture=vi }"/>
                                        
                                            
                                        
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>
                        <!--THANH TOÁN-->
                        <Grid Grid.Row="2" Background="DarkGoldenrod">
                            <StackPanel Orientation="Horizontal">
                            <!--TỔNG HÓA ĐƠN-->
                            <TextBlock Text="Tổng tiền: "  FontSize="30" Margin="50 0 0 0" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="White"/>

                                <TextBlock Text="{Binding total, StringFormat=C, ConverterCulture=vi }" FontSize="30" Margin="50 0 0 0" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="White"/>
                            </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <!--NÚT THÊM ORDER-->
<<<<<<< HEAD
                                <Button Background="#ff6f00" BorderBrush="Transparent" Content="Thanh toán" Margin="10 10 50 10" HorizontalAlignment="Right" Width="120" Height="40" Command="{Binding xacnhancommand}">
=======

                                <Button Background="#ff6f00" BorderBrush="Transparent" Content="Thanh toán" Margin="10 10 300 10" Width="120" Height="40" Command="{Binding xacnhancommand}">
>>>>>>> origin/hao1
                                </Button>
                                <!--NÚT XÓA-->
                                <Button Background="#ff6f00" BorderBrush="Transparent" Content="Xóa" Margin="10 10 100 10" Width="120" Height="40" Command="{Binding xoasanpham}">
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>
                <Grid x:Name="thongke">
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChucNang}" Value="1">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid DataContext="{StaticResource thongkeVM}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Background="DarkGoldenrod" x:Name="chucnangthongke">
                            
                            <!--CHỨC NĂNG THÔNG KÊ-->
                            <Grid>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center">
                                    <!--Button Doanh Thu-->
                                    <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                     ToolTip="Doanh thu">

                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding BtnDoanhThuCommand}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>

                                        <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-Revenue.png"/>

                                    </Button>
                                    <!--Button San Pham-->
                                    <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                     ToolTip="Sản phẩm">

                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding BtnSanPhamCommand}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>

                                        <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-Product.png"/>

                                    </Button>
                                    <!--Button History-->
                                    <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                     ToolTip="Lịch sử">

                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Click">
                                                <i:InvokeCommandAction Command="{Binding BtnLichSuCommand}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>

                                        <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-History.png"/>

                                    </Button>

                                    <!--<Button Height="40" Width="150" Background="#FF0B4911" BorderBrush="{x:Null}" Margin="15 0 0 0">
                                    <DockPanel>
                                        <Image Width="30" Height="30" Source="UserControl_BanHang/Icons/Thongke.png"/>
                                        <TextBlock Margin="0 7 0 0">SẢN PHẨM</TextBlock>
                                    </DockPanel>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangThongKe}" Value="1">
                                                    <Setter Property="FontWeight" Value="Bold"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnSanPhamCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Button Height="40" Width="150" Background="#FF0B4911" BorderBrush="{x:Null}" Margin="15 0 0 0">
                                    <DockPanel>
                                        <Image Width="30" Height="30" Source="UserControl_BanHang/Icons/Thongke.png"/>
                                        <TextBlock Margin="0 7 0 0">LỊCH SỬ ĐƠN HÀNG</TextBlock>
                                    </DockPanel>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChucNangThongKe}" Value="2">
                                                    <Setter Property="FontWeight" Value="Bold"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnLichSuCommand}">
                                            </i:InvokeCommandAction>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>-->
                                </StackPanel>
                            </Grid>
                            
                        </Grid>
                        <!--NỘI DUNG THỐNG KÊ-->
                        <Grid Grid.Row="1" Background="PaleGoldenrod" x:Name="thongke_noidung">
                            <Grid x:Name="thongke_doanhthu">
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangThongKe}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:InvokeCommandAction Command="{Binding loadcommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>



                                <lvc:CartesianChart Series="{Binding DoanhThu}">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Ngày" FontSize="20" Foreground="Black" Labels="{Binding Ngay}">
                                            <lvc:Axis.Separator>
                                                <lvc:Separator Step="1" IsEnabled="True"/>
                                            </lvc:Axis.Separator>
                                        </lvc:Axis>
                                    </lvc:CartesianChart.AxisX>
                                    <lvc:CartesianChart.AxisY>
                                        <lvc:Axis Title="Doanh thu" FontSize="20" Foreground="Black"/>
                                    </lvc:CartesianChart.AxisY>
                                </lvc:CartesianChart>


                                <!--THÔNG TIN THỐNG KÊ-->
                                <!--<Grid>
                                    <lvc:CartesianChart x:Name="chartdoanhthu">
                                        <lvc:CartesianChart.Series>
                                            <lvc:ColumnSeries Title="Doanh thu" Values="{Binding TongDoanhThu}"/>
                                        </lvc:CartesianChart.Series>
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis Title="Ngày" Labels="{Binding Path=NgayTao,StringFormat=dd-MM-yyyy}">
                                                <lvc:Axis.Separator>
                                                    <lvc:Separator Step="1"/>
                                                </lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis Title="Case Count"/>
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </Grid>-->
                                <!--<ListView ItemsSource="{Binding doanhthulist}">
                                    -->
                                <!--ĐỊNH DẠNG NỘI DUNG-->
                                <!--
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                            <Setter Property="FontSize" Value="15"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    
                                    -->
                                <!--<ListView.View>
                                        <GridView>
                                            -->
                                <!--ĐỊNH DẠNG HEADER-->
                                <!--
                                            <GridView.ColumnHeaderContainerStyle>
                                                <Style TargetType="GridViewColumnHeader">
                                                    <Setter Property="FontSize" Value="20"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                    <Setter Property="Margin" Value="10"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                    <Setter Property="Width" Value="auto"/>
                                                    <Setter Property="Background" Value="#ff8f00"/>
                                                </Style>
                                            </GridView.ColumnHeaderContainerStyle>
                                            <GridViewColumn Width="150" Header="Ngày" DisplayMemberBinding="{Binding Path=NgayTao,StringFormat=dd-MM-yyyy}"/>
                                            <GridViewColumn Width="auto" Header="Số lượng hóa đơn" DisplayMemberBinding="{Binding SoLuongHoaDon }"/>
                                            <GridViewColumn Width="auto" Header="Tổng tiền" DisplayMemberBinding="{Binding TongDoanhThu}"/>

                                        </GridView>
                                    </ListView.View>-->
                                <!--
                                </ListView>-->
                            </Grid>
                            <Grid x:Name="thongke_sanpham">
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangThongKe}" Value="1">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <!--THÔNG TIN THỐNG KÊ-->

                                <Grid DataContext="{StaticResource nkVM}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Loaded">
                                            <i:InvokeCommandAction Command="{Binding loadcommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <ListView ItemsSource="{Binding tonkhoxlist}">

                                        <!--ĐỊNH DẠNG NỘI DUNG-->
                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="{x:Type ListViewItem}">
                                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                                <Setter Property="FontSize" Value="15"/>
                                            </Style>
                                        </ListView.ItemContainerStyle>
                                        <ListView.View>
                                            <GridView>
                                                <!--ĐỊNH DẠNG HEADER-->
                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style TargetType="{x:Type GridViewColumnHeader}">
                                                        <Setter Property="FontSize" Value="20"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                        <Setter Property="Margin" Value="10"/>
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                        <Setter Property="Width" Value="auto"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>
                                                <GridViewColumn Width="200" Header="Mã sản phẩm" DisplayMemberBinding="{Binding IdSanpham}"/>
                                                <GridViewColumn Width="430" Header="Tên sản phẩm" DisplayMemberBinding="{Binding ten }"/>
                                                <GridViewColumn Width="110" Header="Số lượng" DisplayMemberBinding="{Binding soluong}"/>
                                                <GridViewColumn Width="120" Header="Giá" DisplayMemberBinding="{Binding gia}"/>
                                                <GridViewColumn Width="150" Header="Tổng tiền" DisplayMemberBinding="{Binding chucvu}"/>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </Grid>
                            </Grid>
                            <Grid x:Name="thongke_lichsu">

                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangThongKe}" Value="2">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid DataContext="{StaticResource lsdh}">

                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition />
                                    </Grid.RowDefinitions>

                                    <!--THÔNG TIN THỐNG KÊ-->
                                    <Grid Grid.Row="0">
                                        <StackPanel Orientation="Horizontal">
                                        <TextBox HorizontalAlignment="Left"
                                    x:Name="MAHOADON" Margin="10"
                                    FontSize="20" Width="200" Height="30" 
                                    Background="White"
                                    materialDesign:HintAssist.Hint="Mã hóa đơn"
                                    >
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="TextChanged">
                                                    <i:InvokeCommandAction 
                                                    Command="{Binding BarcodeChangedcommand}" 
                                                    CommandParameter="{Binding ElementName=BARCODE}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </TextBox>
                                        <Button Content="Chi tiết hóa đơn" Command="{Binding xemhdcommand}" Width="200" HorizontalAlignment="Right" Margin="20" />
                                        </StackPanel>
                                    </Grid>
                                    <Grid Grid.Row="1">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Loaded">
                                                <i:InvokeCommandAction Command="{Binding loadcommannd}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                        <ListView ItemsSource="{Binding hoadonlist , Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                                            <!--ĐỊNH DẠNG NỘI DUNG-->
                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="{x:Type ListViewItem}">
                                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                                    <Setter Property="FontSize" Value="15"/>
                                                </Style>
                                            </ListView.ItemContainerStyle>
                                            <ListView.View>
                                                <GridView>
                                                    <!--ĐỊNH DẠNG HEADER-->

                                                    <GridView.ColumnHeaderContainerStyle>
                                                        <Style TargetType="{x:Type GridViewColumnHeader}">
                                                            <Setter Property="FontSize" Value="20"/>
                                                            <Setter Property="Foreground" Value="Black"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                            <Setter Property="Margin" Value="10"/>
                                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                                            <Setter Property="IsEnabled" Value="False"/>
                                                            <Setter Property="Width" Value="auto"/>
                                                            <Setter Property="Background" Value="Transparent"/>
                                                        </Style>
                                                    </GridView.ColumnHeaderContainerStyle>
                                                    <GridViewColumn Width="120" Header="Mã" DisplayMemberBinding="{Binding MaHoadon}"/>
                                                    <GridViewColumn Width="230" Header="Thời gian" DisplayMemberBinding="{Binding NgayTao}"/>
                                                    <GridViewColumn Width="200" Header="Tổng tiền" DisplayMemberBinding="{Binding ThanhTien}"/>
                                                    <GridViewColumn Width="150" Header="Khách hàng" DisplayMemberBinding="{Binding bennhan}"/>
                                                    <GridViewColumn Width="150" Header="Tên nhân viên" DisplayMemberBinding="{Binding TenNhanvien}"/>
                                                    <GridViewColumn Width="150" Header="Khuyến mãi" DisplayMemberBinding="{Binding TenChuongtrinh}"/>
                                                    <GridViewColumn Width="150" Header="Trạng thái" DisplayMemberBinding="{Binding trangthai}"/>
                                                </GridView>
                                            </ListView.View>
                                        </ListView>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </Grid>
                <Grid x:Name="sanpham">

                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChucNang}" Value="2">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid DataContext="{StaticResource spVM}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Background="DarkGoldenrod">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <!--NÚT THÊM SẢN PHẨM-->

                                <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Command="{Binding themsanphamcommand}"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                     ToolTip="Thêm sản phẩm">
                                    <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-add.png"/>

                                </Button>

                                <!--NÚT SỬA SẢN PHẨM-->

                                <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                     Command="{Binding suasanphamcommand}"
                                     ToolTip="Sửa sản phẩm">
                                    <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-edit.png"/>

                                </Button>

                                <!--NÚT XÓA SẢN PHẨM-->

                                <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                     ToolTip="Xóa sản phẩm">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction 
                                                Command="{Binding xoacommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-delete.png"/>

                                </Button>

                            </StackPanel>
                        </Grid>
                        <!--THÔNG TIN SẢN PHẨM TẠI CỬA HÀNG-->
                        <Grid Grid.Row="1" Background="PaleGoldenrod">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded">
                                    <i:InvokeCommandAction Command="{Binding loadcomannd}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <ListView ItemsSource="{Binding sanphamlist, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                                <!--ĐỊNH DẠNG NỘI DUNG-->
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="{x:Type ListViewItem}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        <Setter Property="FontSize" Value="15"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.View>
                                    <GridView>
                                        <!--ĐỊNH DẠNG HEADER-->
                                        <GridView.ColumnHeaderContainerStyle>
                                            <Style TargetType="{x:Type GridViewColumnHeader}">
                                                <Setter Property="FontSize" Value="20"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="Margin" Value="10"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="IsEnabled" Value="True"/>
                                                <Setter Property="Width" Value="auto"/>
                                                <Setter Property="Background" Value="Transparent"/>
                                            </Style>
                                        </GridView.ColumnHeaderContainerStyle>
                                        <GridViewColumn Width="250" Header="barcode" DisplayMemberBinding="{Binding IdSanpham}"/>
                                        <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSanpham}"/>
                                        <GridViewColumn Width="150" Header="Đơn giá" DisplayMemberBinding="{Binding Gia}"/>
                                        <GridViewColumn Width="110" Header="Bán chạy">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsEnabled="False"  IsChecked="{Binding SanphamHot}"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="110" Header="Sản phẩm mới">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsEnabled="False"  IsChecked="{Binding SanphamMoi}"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>
                    </Grid>



                </Grid>
                <Grid x:Name="nhaphang">
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChucNang}" Value="4">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid DataContext="{StaticResource nhVM}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition/>
                            
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Background="DarkGoldenrod" x:Name="chucnangnhaphang">
                            <Grid.RowDefinitions>
                                <RowDefinition ></RowDefinition>
                                <RowDefinition></RowDefinition>
                               
                            </Grid.RowDefinitions>
                            <!--CHỨC NĂNG THÔNG KÊ-->
                            <Grid Grid.Row="0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center">
                                <!--Button PEPSI-->
                                <Button

                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Content="PEPSI">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnPepsicoCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </Button>
                                <!--Button COCA-->
                                <Button
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Content="COCACOLA">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnCocaColaCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <!--Button INTERFOOD-->
                                <Button
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Content="INTERFOOD">


                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnInterFoodCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <!--Button REDBULL-->
                                <Button

                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Content="REDBULL">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnRedBullCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </Button>
                                <!--Button TANHIEPPHAT-->
                                <Button

                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Content="TANHIEPPHAT">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnTanHiepPhatCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </Button>
                                <!--Button UniversalRobina-->
                                <Button

                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Content="UniversalRobina">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnUniversalRobinaCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </Button>
                                <!--Button KINHDO-->
                                <Button

                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Content="KINHDO">

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding BtnKinhDoCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                </Button>
                               
                            </StackPanel>
                            </Grid>
                            <Grid Grid.Row="1">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Margin="10" Text="Số lượng: " FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" Foreground="White" />
                                    <TextBox  Margin="10" FontSize="20" Width="200" Height="30" Text="{Binding themsoluong}" Background="White"/>
                                    <Button  Margin="10" Content="Thêm" Width="120" Height="auto" />
                                </StackPanel>
                                
                            </Grid>
                            
                                
                        </Grid>
                        <!--NỘI DUNG NHẬP HÀNG-->
                        <Grid Grid.Row="1" Background="PaleGoldenrod" x:Name="nhaphang_noidung">
                            <Grid x:Name="PEPSI">
                                <Grid.RowDefinitions>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:InvokeCommandAction Command="{Binding loadpepsi}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                            <!--NỘI DUNG NHẬP HÀNG-->
                                <Grid Grid.Row="0">
                                    <ListView ItemsSource="{Binding pepsilist}">
                                        <ListView.View>
                                            <GridView>

                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style TargetType="GridViewColumnHeader">
                                                        <Setter Property="FontSize" Value="20"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                        <Setter Property="Margin" Value="10"/>
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                        <Setter Property="Width" Value="auto"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>
                                                <!--<GridViewColumn Width="150" Header="ID đối tác" DisplayMemberBinding="{Binding IdNhaCC}"/>-->
                                                <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSP }"/>
                                                <GridViewColumn Width="auto" Header="Số lượng" DisplayMemberBinding="{Binding SoLuong}"/>

                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </Grid>
                                <Grid VerticalAlignment="Bottom" Grid.Row="1">
                                    <Button Content="Nhập" Width="120" Margin="60" Command="{Binding nhapPepsi}"/>
                                </Grid>



                                <!--THÔNG TIN THỐNG KÊ-->
                                <!--<Grid>
                                    <lvc:CartesianChart x:Name="chartdoanhthu">
                                        <lvc:CartesianChart.Series>
                                            <lvc:ColumnSeries Title="Doanh thu" Values="{Binding TongDoanhThu}"/>
                                        </lvc:CartesianChart.Series>
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis Title="Ngày" Labels="{Binding Path=NgayTao,StringFormat=dd-MM-yyyy}">
                                                <lvc:Axis.Separator>
                                                    <lvc:Separator Step="1"/>
                                                </lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis Title="Case Count"/>
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </Grid>-->
                                <!--<ListView ItemsSource="{Binding doanhthulist}">
                                    -->
                                <!--ĐỊNH DẠNG NỘI DUNG-->
                                <!--
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                            <Setter Property="FontSize" Value="15"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    
                                    -->
                                <!--<ListView.View>
                                        <GridView>
                                            -->
                                <!--ĐỊNH DẠNG HEADER-->
                                <!--
                                            <GridView.ColumnHeaderContainerStyle>
                                                <Style TargetType="GridViewColumnHeader">
                                                    <Setter Property="FontSize" Value="20"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                    <Setter Property="Margin" Value="10"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                    <Setter Property="Width" Value="auto"/>
                                                    <Setter Property="Background" Value="#ff8f00"/>
                                                </Style>
                                            </GridView.ColumnHeaderContainerStyle>
                                            <GridViewColumn Width="150" Header="Ngày" DisplayMemberBinding="{Binding Path=NgayTao,StringFormat=dd-MM-yyyy}"/>
                                            <GridViewColumn Width="auto" Header="Số lượng hóa đơn" DisplayMemberBinding="{Binding SoLuongHoaDon }"/>
                                            <GridViewColumn Width="auto" Header="Tổng tiền" DisplayMemberBinding="{Binding TongDoanhThu}"/>

                                        </GridView>
                                    </ListView.View>-->
                                <!--
                                </ListView>-->
                            </Grid>
                            <Grid x:Name="COCA">
                                <Grid.RowDefinitions>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="1">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <!--THÔNG TIN THỐNG KÊ-->

                                <Grid DataContext="{StaticResource nhVM}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Loaded">
                                            <i:InvokeCommandAction Command="{Binding loadcoca}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <Grid Grid.Row="0">
                                        <ListView ItemsSource="{Binding cocalist}">
                                            <ListView.View>
                                                <GridView>

                                                    <GridView.ColumnHeaderContainerStyle>
                                                        <Style TargetType="GridViewColumnHeader">
                                                            <Setter Property="FontSize" Value="20"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                            <Setter Property="Margin" Value="10"/>
                                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                                            <Setter Property="IsEnabled" Value="True"/>
                                                            <Setter Property="Width" Value="auto"/>
                                                            <Setter Property="Background" Value="Transparent"/>
                                                        </Style>
                                                    </GridView.ColumnHeaderContainerStyle>
                                                    <!--<GridViewColumn Width="150" Header="ID đối tác" DisplayMemberBinding="{Binding IdNhaCC}"/>-->
                                                    <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSP }"/>
                                                    <GridViewColumn Width="auto" Header="Số lượng" DisplayMemberBinding="{Binding SoLuong}"/>

                                                </GridView>
                                            </ListView.View>
                                        </ListView>
                                    </Grid>
                                    <Grid VerticalAlignment="Bottom" Grid.Row="1">
                                        <Button Content="Nhập" Width="120" Margin="60" Command="{Binding nhapCoca}"/>
                                    </Grid>
                                </Grid>
                            </Grid>
                            <Grid x:Name="INTERFOOD">

                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="2">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid DataContext="{StaticResource nhVM}">


                                    <!--THÔNG TIN THỐNG KÊ-->
                            
                                    <Grid Grid.Row="0">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="Loaded">
                                                <i:InvokeCommandAction Command="{Binding loadinterfood}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                        <Grid>
                                            <ListView ItemsSource="{Binding interfoodlist}">
                                                <ListView.View>
                                                    <GridView>

                                                        <GridView.ColumnHeaderContainerStyle>
                                                            <Style TargetType="GridViewColumnHeader">
                                                                <Setter Property="FontSize" Value="20"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                                <Setter Property="Margin" Value="10"/>
                                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                                <Setter Property="IsEnabled" Value="True"/>
                                                                <Setter Property="Width" Value="auto"/>
                                                                <Setter Property="Background" Value="Transparent"/>
                                                            </Style>
                                                        </GridView.ColumnHeaderContainerStyle>
                                                        <!--<GridViewColumn Width="150" Header="ID đối tác" DisplayMemberBinding="{Binding IdNhaCC}"/>-->
                                                        <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSP }"/>
                                                        <GridViewColumn Width="auto" Header="Số lượng" DisplayMemberBinding="{Binding SoLuong}"/>

                                                    </GridView>
                                                </ListView.View>
                                            </ListView>
                                        </Grid>
                                    </Grid>
                                    <Grid VerticalAlignment="Bottom" Grid.Row="1">
                                        <Button Content="Nhập" Width="120" Margin="60" Command="{Binding nhapInter}"/>
                                    </Grid>
                                </Grid>
                            </Grid>
                            <Grid x:Name="REDBULL">
                                <Grid.RowDefinitions>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="3">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:InvokeCommandAction Command="{Binding loadredbull}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <!--NỘI DUNG NHẬP HÀNG-->
                                <Grid Grid.Row="0">
                                    <ListView ItemsSource="{Binding redbulllist}">
                                        <ListView.View>
                                            <GridView>

                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style TargetType="GridViewColumnHeader">
                                                        <Setter Property="FontSize" Value="20"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                        <Setter Property="Margin" Value="10"/>
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                        <Setter Property="Width" Value="auto"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>
                                                <!--<GridViewColumn Width="150" Header="ID đối tác" DisplayMemberBinding="{Binding IdNhaCC}"/>-->
                                                <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSP }"/>
                                                <GridViewColumn Width="auto" Header="Số lượng" DisplayMemberBinding="{Binding SoLuong}"/>

                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </Grid>
                                <Grid VerticalAlignment="Bottom" Grid.Row="1">
                                    <Button Content="Nhập" Width="120" Margin="60" Command="{Binding nhapRedBull}"/>
                                </Grid>
                            </Grid>
                            <Grid x:Name="TANHIEPPHAT">
                                <Grid.RowDefinitions>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="4">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:InvokeCommandAction Command="{Binding loadtanhiepphat}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <!--NỘI DUNG NHẬP HÀNG-->
                                <Grid Grid.Row="0">
                                    <ListView ItemsSource="{Binding tanhiepphatlist}">
                                        <ListView.View>
                                            <GridView>

                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style TargetType="GridViewColumnHeader">
                                                        <Setter Property="FontSize" Value="20"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                        <Setter Property="Margin" Value="10"/>
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                        <Setter Property="Width" Value="auto"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>
                                                <!--<GridViewColumn Width="150" Header="ID đối tác" DisplayMemberBinding="{Binding IdNhaCC}"/>-->
                                                <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSP }"/>
                                                <GridViewColumn Width="auto" Header="Số lượng" DisplayMemberBinding="{Binding SoLuong}"/>

                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </Grid>
                                <Grid VerticalAlignment="Bottom" Grid.Row="1">
                                    <Button Content="Nhập" Width="120" Margin="60" Command="{Binding nhapTanHiepPhat}"/>
                                </Grid>
                            </Grid>
                            <Grid x:Name="UniversaUniversalRobina">
                                <Grid.RowDefinitions>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="5">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:InvokeCommandAction Command="{Binding loadUR}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <!--NỘI DUNG NHẬP HÀNG-->
                                <Grid Grid.Row="0">
                                    <ListView ItemsSource="{Binding URlist}">
                                        <ListView.View>
                                            <GridView>

                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style TargetType="GridViewColumnHeader">
                                                        <Setter Property="FontSize" Value="20"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                        <Setter Property="Margin" Value="10"/>
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                        <Setter Property="Width" Value="auto"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>
                                                <!--<GridViewColumn Width="150" Header="ID đối tác" DisplayMemberBinding="{Binding IdNhaCC}"/>-->
                                                <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSP }"/>
                                                <GridViewColumn Width="auto" Header="Số lượng" DisplayMemberBinding="{Binding SoLuong}"/>

                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </Grid>
                                <Grid VerticalAlignment="Bottom" Grid.Row="1">
                                    <Button Content="Nhập" Width="120" Margin="60" Command="{Binding nhapUniver}"/>
                                </Grid>
                            </Grid>
                            <Grid x:Name="KinhDo">
                                <Grid.RowDefinitions>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="6">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Loaded">
                                        <i:InvokeCommandAction Command="{Binding loadkinhdo}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <!--NỘI DUNG NHẬP HÀNG-->
                                <Grid Grid.Row="0">
                                    <ListView ItemsSource="{Binding kinhdolist}">
                                        <ListView.View>
                                            <GridView>

                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style TargetType="GridViewColumnHeader">
                                                        <Setter Property="FontSize" Value="20"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                        <Setter Property="Margin" Value="10"/>
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                        <Setter Property="Width" Value="auto"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>
                                                <!--<GridViewColumn Width="150" Header="ID đối tác" DisplayMemberBinding="{Binding IdNhaCC}"/>-->
                                                <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSP }"/>
                                                <GridViewColumn Width="auto" Header="Số lượng" DisplayMemberBinding="{Binding SoLuong}"/>

                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </Grid>
                                <Grid VerticalAlignment="Bottom" Grid.Row="1">
                                    <Button Content="Nhập" Width="120" Margin="60" Command="{Binding nhapKinhDo}"/>
                                </Grid>
                            </Grid>
                        </Grid>
                        
                    </Grid>
                </Grid>
                <Grid x:Name="nhanvien">
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChucNang}" Value="3">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid  DataContext="{StaticResource nvVM}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <!--CHỨC NĂNG THÊM XÓA SỬA SẢN PHẨM-->
                        <Grid Grid.Row="0" Background="DarkGoldenrod">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <!--NÚT THÊM NHÂN VIÊN-->
                                
                                <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     Command="{Binding themnhanviencommand}"
                                    Background="Transparent"
                                    BorderBrush="Transparent">
                                    <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-add.png"/>

                                </Button>
                                <!--NÚT SỬA NHÂN VIÊN-->
                                <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                     Command="{Binding suanhanviencommand}"
                                     ToolTip="Sửa sản phẩm">
                                    <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-edit.png"/>

                                </Button>
                                <!--<Button
                                    Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                    Background="Transparent"
                                    BorderBrush="Transparent" >
                                    <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-add.png"/>
                                    >
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction 
                                                Command="{Binding suanhanviencommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>-->
                                <!--NÚT XÓA NHÂN VIÊN-->
                                <Button
                                     Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                     HorizontalAlignment="Left"
                                    Margin="20 10 20 10"
                                     
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                     ToolTip="Xóa sản phẩm">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction 
                                                Command="{Binding xoacommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <Image Stretch="Fill" Source="UserControl_BanHang/Icons/icon-delete.png"/>

                                </Button>
                                <!--<Button Content="Xóa" Margin="0 0 20 0">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction 
                                                Command="{Binding xoacommand}"/>

                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>-->
                            </StackPanel>
                        </Grid>
                        <!--THÔNG TIN NHÂN VIÊN-->
                        <Grid Grid.Row="1" Background="PaleGoldenrod">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Loaded">
                                    <i:InvokeCommandAction Command="{Binding loadcommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <Grid>
                                <ListView ItemsSource="{Binding nhanvienlist, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                                    <!--ĐỊNH DẠNG NỘI DUNG-->
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                            <Setter Property="FontSize" Value="15"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.View>
                                        <GridView>
                                            <!--ĐỊNH DẠNG HEADER-->
                                            <GridView.ColumnHeaderContainerStyle>
                                                <Style TargetType="GridViewColumnHeader">
                                                    <Setter Property="FontSize" Value="20"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                    <Setter Property="Margin" Value="10"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                    <Setter Property="Width" Value="auto"/>
                                                    <Setter Property="Background" Value="Transparent"/>
                                                </Style>
                                            </GridView.ColumnHeaderContainerStyle>
                                            <!--<GridViewColumn Width="200" Header="Hình ảnh">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Image Source="{Binding hinhnhanvien}"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>-->
                                            <GridViewColumn Width="250" Header="Mã nhân viên" DisplayMemberBinding="{Binding Manv }"/>
                                            <GridViewColumn Width="250" Header="Tên nhân viên" DisplayMemberBinding="{Binding ten }"/>
                                            <GridViewColumn Width="230" Header="Ngày bắt đầu làm việc" DisplayMemberBinding="{Binding Path=ngaysinh,StringFormat=dd-MM-yyyy}"/>

                                            <GridViewColumn Width="500" Header="Địa chỉ" DisplayMemberBinding="{Binding diachi}"/>
                                            <GridViewColumn Width="120" Header="Chức vụ" DisplayMemberBinding="{Binding chucvu}"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                            </Grid>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
<!--<Grid.Style>
    <Style TargetType="{x:Type Grid}">
        <Setter Property="Visibility" Value="Hidden"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding ChucNang}" Value="4">
                <Setter Property="Visibility" Value="Visible"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
</Grid.Style>
<Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="auto"></RowDefinition>
        <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>
    <Grid Grid.Row="0" Background="DarkGoldenrod">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        --><!--NÚT NHÀ CC--><!--
        <Grid Grid.Row="0">
            <StackPanel>
                --><!--Button Pepsico--><!--
                <Button                                                                                                         
                                Width="130"
                                 Margin="10 10 900 10"
                                Content="Pepsico"
                                ToolTip="Resource name: MaterialDesignRaisedButton">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding BtnPepsicoCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                --><!--Button CocaCola--><!--
                <Button
                                Width="130"
                                 Margin="10 10 600 10"
                                Content="CocaCola"
                                ToolTip="Resource name: MaterialDesignRaisedButton">

                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding BtnCocaColaCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                --><!--Button InterFood--><!--
                <Button
                                Width="130"
                                Margin="10 10 300 10"
                                Content="InterFood"
                                ToolTip="Resource name: MaterialDesignRaisedButton">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding BtnInterFoodCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                --><!--Button Red bull--><!--
                <Button
                                Width="130"
                                Margin="10"
                                Content="Red bull"
                                ToolTip="Resource name: MaterialDesignRaisedButton">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding BtnRedBullCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                --><!--Button TanHiepPhat--><!--
                <Button
                                Width="130"
                                 Margin="10 10 -300 10"
                                Content="TanHiepPhat"
                                ToolTip="Resource name: MaterialDesignRaisedButton">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding BtnTanHiepPhatCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                --><!--Button UniversalRobina--><!--
                <Button
                                Width="150"
                                 Margin="10 10 -600 10"
                                Content="UniversalRobina"
                                ToolTip="Resource name: MaterialDesignRaisedButton">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding BtnUniversalRobinaCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                --><!--Button KinhDo--><!--
                <Button
                                Width="100"
                                 Margin="10 10 -900 10"
                                Content="Kinh đô"
                                ToolTip="Resource name: MaterialDesignRaisedButton">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding BtnKinhDoCommand}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </StackPanel>
        </Grid>
        --><!--NÚT NHẬP SỐ LƯỢNG--><!--
        <Grid Grid.Row="1" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="30 0 0 0" Text="Số lượng: " FontSize="36" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" Foreground="White" />
                <TextBox x:Name="SOLUONGSP" FontSize="20" Width="200" Height="30" Text="{Binding themsoluongsp}" Background="White">
                </TextBox>
                --><!--NÚT NHẬP SỐ LƯỢNG--><!--
                <Button Content="Nhập" Background="#ff6f00" BorderBrush="Transparent" Margin="10 0 0 0" Width="120" Height="30" Command="{Binding editsoluongnhap}" CommandParameter="{Binding ElementName=SOLUONG}">
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
    --><!--NỘI DUNG NHẬP HÀNG--><!--
    <Grid Grid.Row="1"  Background="PaleGoldenrod">
        <Grid x:Name="nhaphang_pepsico">
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="0">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding BtnPepsicoCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView ItemsSource="{Binding sanphamlist, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                --><!--ĐỊNH DẠNG NỘI DUNG--><!--
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="15"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        --><!--ĐỊNH DẠNG HEADER--><!--
                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="FontSize" Value="20"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Margin" Value="10"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="IsEnabled" Value="True"/>
                                <Setter Property="Width" Value="auto"/>
                                <Setter Property="Background" Value="#ff8f00"/>
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>
                        <GridViewColumn Width="250" Header="barcode" DisplayMemberBinding="{Binding IdSanpham }"/>
                        <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSanpham }"/>
                        <GridViewColumn Width="150" Header="Đơn giá" DisplayMemberBinding="{Binding Gia }"/>
                        <GridViewColumn Width="110" Header="Bán chạy">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsEnabled="False"  IsChecked="{Binding SanphamHot}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="110" Header="Sản phẩm mới">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsEnabled="False"  IsChecked="{Binding SanphamMoi}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <Grid x:Name="nhaphang_cocacola">
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="1">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding BtnCocaColaCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView ItemsSource="{Binding sanphamlist, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                --><!--ĐỊNH DẠNG NỘI DUNG--><!--
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="15"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        --><!--ĐỊNH DẠNG HEADER--><!--
                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="FontSize" Value="20"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Margin" Value="10"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="IsEnabled" Value="True"/>
                                <Setter Property="Width" Value="auto"/>
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>
                        <GridViewColumn Width="250" Header="barcode" DisplayMemberBinding="{Binding IdSanpham }"/>
                        <GridViewColumn Width="250" Header="Tên sản phẩm" DisplayMemberBinding="{Binding TenSanpham }"/>
                        <GridViewColumn Width="150" Header="Đơn giá" DisplayMemberBinding="{Binding Gia }"/>
                        <GridViewColumn Width="150" Header="Số lượng" DisplayMemberBinding="{Binding Soluong}"/>
                        <GridViewColumn Width="150" Header="Bán chạy">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsEnabled="False"  IsChecked="{Binding SanphamHot}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="110" Header="Sản phẩm mới">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsEnabled="False"  IsChecked="{Binding SanphamMoi}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        <Grid x:Name="nhaphang_interfood">
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="2">
                            <Setter Property="Visibility" Value="Visible"></Setter>

                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding BtnInterFoodCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Grid>
        <Grid x:Name="nhaphang_redpull">
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="3">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding BtnRedBullCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Grid>
        <Grid x:Name="nhaphang_tanhiepphat">
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="4">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding BtnTanHiepPhatCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Grid>
        <Grid x:Name="nhaphang_universalrobina">
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="5">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding BtnUniversalRobinaCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Grid>
        <Grid x:Name="nhaphang_kinhdo">
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ChucNangNhapHang}" Value="6">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding BtnKinhDoCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Grid>
    </Grid>
</Grid>-->